import { Router } from 'express'; import * as controller from '../controllers/categoryController.js'; import { protect, adminOnly } from '../middleware/auth.js'; const router = Router(); router.route('/').get(controller.listCategories).post(protect, adminOnly, controller.createCategory); router.route('/:id').put(protect, adminOnly, controller.updateCategory).delete(protect, adminOnly, controller.deleteCategory); export default router;
